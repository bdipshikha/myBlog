<head>
		<link href='https://fonts.googleapis.com/css?family=Droid+Serif|Lobster|Yanone+Kaffeesatz|Crimson+Text' rel='stylesheet' type='text/css'>
</head>
<style>
	body {
		margin-top: 70px;
		padding: 8px;
		font-family: Crimson Text;
		font-size: 17px;
	}
	hr {
		border-color: #b3b3b3;
		/*width: 90%;*/
	}
	h2 {
		text-align: center;
	}
</style>
		<div class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand">Dipshikha's Blog</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
          	<li><%= link_to 'Home', '/categories' %></li>
          	<% if current_user %>
							<li><a method="GET" href="/logout">Logout</a></li>
						<% end %>
						<li><%= link_to 'Login', '/sessions/new' %></li>
						<li><%= link_to 'Sign Up', '/users/new' %></li>
          </ul>
        </div>
      </div>
   	</div>

	<div class = "row" %>
		<div class = "col-md-8 col-md-offset-2">
		<h2> Archive Page</h2>
		<h3><%= @month %></h3>
		<% @posts.each do |post| %>
			<a href="/categories/<%= post.category_id %>/posts/<%= post.id %>">
			<h3><%= post.title %></h3>
				<img style = "width:100%;height:200px;" src="<%= post.image %>"></br>
				<p style="width:100%"><%= post.content[0...150] %>....</a></p>
		<hr>
		<% end %>
	</div>
	</div>

	
								





